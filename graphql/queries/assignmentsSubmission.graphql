query AssignmentsSubmission ($assignmentID: ID) {
    assignmentSubmissions (assignmentID: $assignmentID) {
        edges {
            node {
                files
                submittedAt
            }
        }
    }
}