query Assignments ($dueDate: Time!) {
    assignments (where: {dueDate: $dueDate}) {
        edges {
            node {
                name,
                description,
                dueDate,
                class {
                    name
                },
                isExam
            }
        }
    }
}