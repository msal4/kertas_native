query Assignments ($where: AssignmentWhereInput) {
  assignments (where: $where) {
    edges {
      node {
        ...Assignment
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
    totalCount
  }
}
