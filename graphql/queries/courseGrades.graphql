query CourseGrades ($classID: ID) {
  courseGrades (classID: $classID) {
    edges {
      node {
        ...CourseGrade
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
    totalCount
  }
}
