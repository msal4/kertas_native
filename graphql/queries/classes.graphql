query Classes {
    classes {
        edges {
            cursor
            node {
                id
                name
                active
                createdAt
                updatedAt
            }
        }
        pageInfo {
            hasNextPage
            hasPreviousPage
        }
        totalCount
    }
}
