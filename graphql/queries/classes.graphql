query Classes {
    classes {
        edges {
            cursor
            node {
                id
                name
                active
                createdAt
                updatedAt
                teacher {
                    name
                }
            }
        }
        pageInfo {
            hasNextPage
            hasPreviousPage
        }
        totalCount
    }
}
