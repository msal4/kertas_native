query Classes {
  classes {
    edges {
      cursor
      node {
        id
        name
        active
        createdAt
        updatedAt
        teacher {
          id
          name
        }
      }
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
    }
    totalCount
  }
}
