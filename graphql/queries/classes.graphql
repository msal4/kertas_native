query Classes {
  classes {
    edges {
      node {
        ...Class
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
    totalCount
  }
}

fragment Class on Class {
  id
  name
  active
  createdAt
  updatedAt
  teacher {
    id
    name
  }
}
