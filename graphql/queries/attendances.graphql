query Attendances ($where: AttendanceWhereInput) {
  attendances (where: $where) {
    edges {
      node {
      	...Attendance
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
    totalCount
  }
}
