query Attendances ($after: Cursor, $where: AttendanceWhereInput) {
  attendances (after: $after, where: $where) {
    edges {
      node {
      	...Attendance
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
    totalCount
  }
}
